import { PremiumCalculationInput } from './premium-calculation-input';

describe('PremiumCalculationInput', () => {
  it('should create an instance', () => {
    expect(new PremiumCalculationInput()).toBeTruthy();
  });
});
